name: My Workflow

on:
  push:
    branches: [ main ]

jobs:               # <--- MUST exist
  deploy:           # <--- Job name must be inside jobs
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
